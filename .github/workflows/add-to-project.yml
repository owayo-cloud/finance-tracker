name: 📌 Add to Project

# Disabled workflow - this was for the FastAPI org project board
# To enable: update project-url to your own project and set PROJECTS_TOKEN secret
on:
  workflow_dispatch:

jobs:
  add-to-project:
    name: Add to project
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "This workflow is disabled. To enable it:"
          echo "1. Create a GitHub Project in your account/org"
          echo "2. Update project-url in this workflow"
          echo "3. Create a PROJECTS_TOKEN secret with project write permissions"
          echo "4. Change the 'on' trigger back to pull_request_target/issues"
